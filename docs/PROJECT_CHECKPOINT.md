# SLAY THE MONSTARZ — 최종 작업 체크포인트

- 저장일: 2026-06-28
- 프로젝트 루트: `C:\Users\silve\OneDrive\Desktop\slay-the-monstarz`
- 현재 단계: 3막 플레이 가능, 전 막 10층·기존 적 2체전 완료, 게임 구성 완성도 보강
- 용도: 새 Codex/Claude 세션에서 가장 먼저 읽는 단일 진입 문서

## 0. 2026-07-01 전 막 10층·2체전 최신 체크포인트

- 각 막은 10층이다:
  `전투-전투-이벤트-2체전-상점/엘리트-2체전-보물-2체전-휴식-보스`.
- 4·6·8층은 기존 막별 일반 적을 2체씩 조합한다. 신규 적과 이미지는 없다.
- `BattleEngine`·`BattleScreen`·`BattleStage`가 동시 2체 상태, 의도,
  연속 행동, 집중 대상 전환, 적별 연출과 HUD를 지원한다.
- 구형 저장 호환을 위해 기존 노드 ID 의미를 유지하고 삽입 노드는
  `f3b`·`f4b`·`f5b`를 사용한다.
- 실제 브라우저에서 10층 지도와 4층 2체전, 두 적 연속 행동을 검수했다.
- typecheck, 10파일 116테스트, production build PASS.
- 이후 신규 이미지는 `docs/IMAGE_REQUESTS.md`에 사양만 기록하고
  게임 구성·규칙·콘텐츠 완성을 우선한다.

## 0.1 2026-07-01 카드 아트 체크포인트

- `abc`의 신규 카드 아트 60종을 공용 카드 40종·활성 캐릭터 시그니처
  20종에 전부 연결했다. 라이브 경로는 `assets/game/cards/`다.
- 15종의 밝은 외곽선을 제거했고 Rang·72 시그니처의 반대 종족 테마
  연결을 수정했다. 원본 `abc`는 변경하지 않았다.
- 최종 에셋은 전부 1024×1024 불투명 RGB이며 누락·초과·중복은 0,
  라이브 SHA-256 대조는 60/60 일치한다.
- 리뷰·재현 기록: `assets/review/cards-abc-v1/`.
- 이전 라이브 17종 백업: `assets/game/cards-backup/20260701-110102/`.
- 실제 전투 손패에서 일반 카드와 Rang·72 시그니처 렌더링을 확인했다.
- py_compile, typecheck, 112테스트, production build PASS.

## 0.2 2026-07-01 캐릭터 공격 모션 체크포인트

- 활성 캐릭터는 20명(T6·P5·Z9)이며 모두 전용 5×5, 25프레임 공격
  스프라이트를 `assets/game/character-motion/`에서 사용한다.
- 정적 대기는 기존 개별 컷아웃을 유지하고 공격 때만 전용 시트로 전환한다.
- 플레이어 실제 높이는 지두두 기준 208.38px로 통일돼 적과의 체급 및
  대기↔공격 크기가 일치한다.
- 공격 프레임은 흰 체크 배경·닫힌 흰 틈·프레임 번호·셀 혼입을 제거했고,
  몸통 중심과 지면 접점을 안정화했다.
- 최종 에셋 QA 20/20, typecheck, 112테스트, production build,
  `?combatQa=1` 지두두·김민철 화면 검수를 통과했다.
- 재가공 도구: `tools/character_attack_sheets.py`.
- 리뷰 기록: `assets/review/character-attack-cwe-v1/`.

## 1. 절대 유지할 기획

- 게임명은 **SLAY THE MONSTARZ**.
- Slay the Spire 계열의 턴제 로그라이크 덱빌딩을 HTML 게임으로 가볍게 재구성한다.
- MONSTARZ SURVIVORS의 시스템이나 콘텐츠는 섞지 않는다.
- 목표 플레이타임은 한 판 20~30분이다.
- MONSTARZ는 캐릭터·팀 정체성으로 입히고, 전투·지도·덱빌딩 구조는 SLAY THE MONSTARZ만의 독립 게임으로 만든다.
- 캐릭터 카드/로스터 이미지는 정면이어도 되지만 전투 스프라이트는 서로 마주보는 측면 또는 3/4 방향을 사용한다. 공용 능력 카드 아트는 특정 캐릭터 초상을 사용하지 않는다.
- 활성 기존 멤버 18명은 제공된 레퍼런스의 얼굴과 헤어 정체성을 유지한다.
- 변현제는 `assets/refs/mini.webp`, 김민철은 `assets/refs/soulkey.jpg` 실제 사진을 바탕으로 기존 레퍼런스와 같은 캐릭터 계열로 제작한다.
- 캐릭터 얼굴만 정체성 기준으로 고정하고 복장·장비·포즈는 게임 분위기와 종족 테마에 맞춘다.
- 잘린 원본은 패딩으로 복구하지 않는다. 여백이 포함된 원화부터 다시 생성한다.
- 이미지나 모션 생성 결과는 사용자의 승인 없이 합격·라이브 승격 처리하지 않는다.

## 2. 현재 완료된 결과

### 전투 구현

- React + TypeScript + Vite UI.
- PixiJS 전투 렌더링.
- 렌더러와 분리된 순수 TypeScript 전투 엔진.
- 에너지, 드로우/버린 더미, 손패, 카드 사용, 피해, 방어도, 상태, 적 의도, 승패.
- 일반전/보스전 전환.

### 라이브 전투 에셋

- 플레이어 마스터: `assets/game/jdd_battle.png`
- 플레이어 대기: `assets/game/jdd_idle_sheet.png`
- 플레이어 공격: `assets/game/jdd_attack_sheet.png`
- 일반 적: `assets/game/sentinel_scout.png`
- 보스: `assets/game/brood_queen.png`
- 일반 적 모션: `assets/game/sentinel_scout_motion_sheet.png`
- 보스 모션: `assets/game/brood_queen_motion_sheet.png`
- 1막 배경: `assets/game/stage_bg_act1.png`

### 전투 연출

- 지두두 대기·공격 AnimatedSprite.
- 공격 시트와 대기 시트의 크기·발 기준선 통일.
- 총구 섬광을 별도 Pixi VFX로 분리.
- 일반 적 원거리 차지·투사체·피격·사망.
- 보스 근접 런지·피격·사망.
- 일반 적·보스 대기 2포즈, 공격 3포즈, 피격 1포즈 `AnimatedSprite`.
- 피해량별 경량/중량/강타 3단계 타격 정책.
- 히트스톱, 피격 플래시, 임팩트 광선·링·파편, 넉백, 피해 숫자 펀치, 피해 비례 화면 흔들림.
- 플레이어와 적 공용 육각 방어 실드.
- 다단 공격의 타격별 실제 피해 전달. 연사는 `-3`, `-3`으로 분리.
- 겹치는 흔들림·넉백이 좌표를 덮어쓰지 않도록 실행 번호로 보호.

### 검증

- `npm.cmd run typecheck` 통과.
- `npm.cmd run test` 통과: 1파일, 2테스트.
- `npm.cmd run build` 통과.
- 브라우저에서 중사격, 연사, 플레이어 방어, 적 원거리 공격, 적 방어 확인.
- 공격·피해 숫자·실드 잘림 없음.
- 브라우저 콘솔 오류 0.

## 3. 주요 문서

새 세션은 아래 순서로 읽는다.

1. `docs/PROJECT_CHECKPOINT.md`
2. `docs/STATUS.md`
3. `docs/COMBAT_FEEL_POLICY.md`
4. `docs/GAME_DESIGN.md`
5. `docs/ART_DIRECTION.md`
6. `docs/PLAN.md`
7. `docs/HANDOFF.md`

## 4. 현재 주요 코드

- `src/ui/BattleScreen.tsx`: 전투 UI, 엔진 이벤트와 Pixi 스테이지 연결.
- `src/game/engine/BattleEngine.ts`: 결정론적 전투 상태와 이벤트.
- `src/game/engine/BattleEngine.test.ts`: 다단 공격과 방어도 흡수 검증.
- `src/game/pixi/BattleStage.ts`: 스프라이트, 적 행동, 전투 VFX.
- `src/content/battleContent.ts`: 현재 카드·캐릭터·적 데이터.
- `tools/keypose_board.py`: GPT/Nano Banana 키포즈 보드 크로마 제거·셀 분리·안전 여백 QA.
- `tools/aether_assets.py`: 과거 지두두 Aether 산출물 처리용 레거시 도구. 신규 생성에는 사용하지 않는다.

## 5. 남은 작업 — 권장 순서

한 번에 전부 진행하지 말고 각 묶음 시작 전에 사용자와 범위를 합의한다.

### A. 현재 수직 슬라이스 최종 승인

1. 사용자가 현재 타격감과 전투 화면을 직접 확인한다.
2. 필요하면 수치만 조정한다: 히트스톱, 흔들림, 넉백, 숫자 크기.
3. 이후 아래 B 또는 C 중 하나를 선택한다.

### B. 전투 수직 슬라이스 추가 폴리시

1. 상태이상 적용·해제 VFX.
2. 카드 속성별 색상과 투사체 규칙.
3. 보스 강공격 텔레그래프와 페이즈 연출.
4. 타격음·총성·실드음 및 음량 정책.
5. 화면 흔들림 강도와 모션 감소 옵션.

이 단계는 선택 사항이다. 1막 시스템 제작을 먼저 시작해도 된다.

### C. 2차 목표 — 1막 풀루프 MVP

1. 런 상태와 콘텐츠 스키마 확정.
2. 20~30분에 맞는 1막 분기 지도 생성.
3. 전투 노드에서 보상 화면으로 이어지는 진행 흐름.
4. 카드 12~16장과 카드 3중1 보상·스킵.
5. 일반 적 총 3종, 엘리트 1종, 1막 보스 1종.
6. 휴식, 상점, 이벤트, 보물 노드.
7. 유물 5종, 포션 2종, 골드 경제.
8. 덱 보기, 카드 강화·제거.
9. 버전 필드가 있는 localStorage 저장·이어하기.
10. 시드 재현성, 패배·승리·재시작, 20~30분 밸런스 QA.

### D. 캐릭터 확장

1. 기존 18명과 `mini`, `soulkey`를 포함한 활성 20명 메타데이터 정리.
2. 얼굴·헤어 기준 시트와 종족/역할/전투복 방향을 사용자와 확정.
3. 승인된 마스터 이미지를 먼저 만든 뒤 GPT/Nano Banana 키포즈 보드로 필요한 동작만 제작.
4. 캐릭터별 시작 덱, 고유 메커니즘, 카드 풀을 순차 추가.
5. 20명을 한 번에 생성하지 않고 한 명씩 검수·승격한다.

### E. 적과 보스 확장

- 일반 적과 엘리트는 스타크래프트 유닛을 모티브로 재해석한다.
- 부유 파수병·군체 여왕 6포즈 보드는 `assets/review/enemy-motion-gpt-board-v1/`에 원본·QA 기록이 있으며 라이브 연동까지 완료했다.
- 확정 보스 후보 6종:
  - P: 캐리어, 아칸
  - T: 배틀크루저, 시즈탱크
  - Z: 울트라, 퀸
- 원작 유닛을 그대로 복제하지 말고 SLAY THE MONSTARZ 세계관에 맞는 독자 디자인으로 만든다.
- 현재 라이브 보스는 군체 여왕 1종이며 나머지는 미제작이다.

### F. 3차 이후

1. 2막·3막 지도와 적·이벤트·보스.
2. 난이도 상승 체계와 해금.
3. 전체 오디오·접근성·설정.
4. 성능 최적화와 모바일/저사양 대응.
5. Vercel 등 정적 호스팅 배포.
6. 최종 회귀 테스트와 콘텐츠 밸런스.

## 6. 미실행 상태

- Supabase/CDN 업로드 없음.
- 외부 배포 없음.
- 적·보스 고품질 키포즈 모션은 라이브 적용 완료.
- 1막 지도·보상·상점·이벤트·유물·포션 미구현.
- 캐릭터 20명 전투 캐릭터화 완료. `pado`, `peanut`은 2026-07-01 게임에서 삭제.
- 사운드 미구현.

## 7. 새 세션 시작 명령

```powershell
Set-Location 'C:\Users\silve\OneDrive\Desktop\slay-the-monstarz'
Get-Content -Encoding utf8 -Raw '.\docs\PROJECT_CHECKPOINT.md'
npm.cmd run typecheck
npm.cmd run test
npm.cmd run build
```

PowerShell에서 한글이 깨지면 반드시 `Get-Content -Encoding utf8`을 사용한다.

## 8. 다음 담당자에게 줄 한 줄

> 이 문서와 `docs/STATUS.md`, `docs/HANDOFF.md`를 읽고, 검증된 `지도 → 전투 → 카드 보상 → 지도` 흐름을 보존한 채 6층 휴식 노드의 회복/카드 강화 선택부터 연결하라. 신규 이미지에는 Aether를 사용하지 말고, 설치된 `$studio`를 시작점으로 사용한다.

## 9. 2026-06-28 종료 체크포인트 — 1막 런 기반과 호환 플러그인

### 완료·검증된 기반

- `RunEngine`과 관련 타입·7층 분기 지도·카드 보상 테스트를 추가했다.
- 런 상태는 HP, 최대 HP, 골드, 덱, 현재/완료/도달 가능 노드, 보상, 승패 단계를 보유한다.
- `BattleEngine`은 선택적 `playerHp`와 `deck`을 받아 런 전투를 시작할 수 있다.
- 보상 후보 카드 4장을 추가했다.
- typecheck, 테스트 5개, 프로덕션 빌드가 통과했다.

### 당시 아직 연결되지 않았던 것

- `RunEngine` React 연결, 지도·보상 UI, 전투 승리 HP 반환과 다음 노드 진행은 아래 10절에서 해소했다.
- 당시 휴식·상점·이벤트·보물·엘리트·저장/이어하기는 미구현이었다. 현재는 12절과 `HANDOFF.md` 15~22절에서 해소됐다.
- 현재 7층 구조와 수치는 구현 초안이지 사용자 최종 승인 기획이 아니다.

### 새 채팅 환경

- 개인 호환 플러그인 `codex-game-studios` 1.0.0이 설치·활성화되어 있다.
- Codex를 재시작한 새 채팅에서 `$studio`를 호출한다.
- 플러그인 소스는 `C:\Users\silve\plugins\codex-game-studios`이며 프로젝트 파일과 분리되어 있다.
- 상세 파일 목록과 정확한 다음 연결 순서는 `docs/HANDOFF.md` 13절을 따른다.

## 10. 2026-06-28 추가 체크포인트 — 최소 런 UI 연결 완료

- 기존 `RunEngine`을 React 앱에 연결했다.
- 앱에서 지도 → 전투 → 카드 보상 3중1/스킵 → 다음 지도 흐름을 실행할 수 있다.
- 전투 시작 시 현재 HP·덱·노드 인카운터·전투 시드를 전달하고, 승리 시 남은 HP를 런에 반환한다.
- 분기 지도는 완료·도달 가능·잠김 노드를 구분하며 현재 7층 초안 경로를 표시한다.
- `BattleScreen`은 런이 생성한 외부 전투 엔진을 렌더한다.
- typecheck, 테스트 3파일 8개, 프로덕션 빌드 통과.
- 브라우저에서 최소 흐름과 콘솔 오류 0을 확인했다.
- 신규 이미지 생성·라이브 에셋 교체·배포는 없었다.
- 다음 묶음은 6층 휴식 노드와 회복/카드 강화 선택이다. 상세 파일과 검증 기록은 `docs/HANDOFF.md` 14절을 따른다.

## 11. 2026-06-28 추가 체크포인트 — OpenCode 중단 인수와 카드 아트 UI

- OpenCode는 휴식 이후 이벤트·상점·유물·보물·엘리트까지 구현·문서화했다.
- 마지막 미문서 작업은 카드 아트 자동 연동과 카드 UI 개편이었다.
- `src/content/cardArt.ts`가 `assets/game/cards/{cardId}.png`를 자동 탐지한다.
- 전투·보상·강화·상점 카드가 동일한 상단 아트/하단 정보 구조를 사용한다.
- 승인 아트가 없을 때 공격/스킬 타입별 절차적 플레이스홀더를 표시한다.
- 상점 마크업 누락, 코스트 오브 잘림, 반복 경로 탐색을 이어받아 수정했다.
- typecheck, 테스트 3파일 40개, 빌드, 브라우저 검증 통과. 콘솔 오류 0.
- 사용 가능한 카드 후보·라이브 카드 PNG는 0장이다. 지두두 중심으로 잘못 생성한 후보 1장은 폐기 보관했다.
- 공용 카드는 캐릭터별이 아니라 `cardId`별 이미지 1장을 공유하며, 캐릭터 전용 이미지는 시그니처 카드만 예외다.
- 카드 이미지 제작은 사용자 결정으로 다른 캐릭터 이미지 일괄 제작 단계까지 보류한다. 상세 규칙은 `docs/HANDOFF.md` 21절을 따른다.

## 12. 2026-06-28 추가 체크포인트 — 자동 저장·이어하기

- `RunState(version: 1)`를 `slay-the-monstarz.run` localStorage 키에 자동 저장한다.
- 시작 화면에서 저장된 층·HP·골드·덱을 확인하고 이어하거나, 2단계 확인 후 새 런으로 교체할 수 있다.
- 지도와 비전투 임시 화면은 마지막 상태를 그대로 복원한다.
- 전투 중 새로고침은 진행 중 턴이 아니라 해당 전투의 턴 1·진입 상태로 복원한다.
- 손상·미지원 버전·알 수 없는 콘텐츠·불일치 노드 저장은 안전하게 폐기한다.
- typecheck, 테스트 4파일 47개, build, 브라우저 이어하기 검증 통과. 콘솔 오류 0.
- 일반 적 3종과 1·2층 인카운터 분산은 아래 13절에서 완료했다. 포션·카드 확충은 `HANDOFF.md` 24~26절에서 완료됐다.

## 13. 2026-06-28 추가 체크포인트 — 일반 적 P/T/Z 3종

- 일반 적은 P 부유 파수병, T 잔해 총잡이, Z 산성 추적자 총 3종이다.
- T는 다단 공격·약화·방어, Z는 취약 표식·강공격·방어로 판단 패턴이 다르다.
- 1·2층에 시드 기반으로 세 적을 모두 배치하고, 도달 가능한 연속 전투에서 같은 적이 반복되지 않는다.
- T/Z는 이미지 제작 보류 방침에 따라 승인된 `scout` 모션을 임시 재사용한다.
- typecheck, 테스트 4파일 53개, build, 브라우저 지도·T 전투 검증 통과. 콘솔 오류 0.
- 포션 2종의 획득·2슬롯 보유·전투 사용·소모·저장 복원은 `HANDOFF.md` 24절에서 완료됐다.

## 14. 2026-06-28 추가 체크포인트 — 클로드 인수 후 혼합 상점 확장

- 클로드가 24~27절에서 포션 2종, 카드 16종, 드로우·에너지·힘·소멸, 1차 밸런스 패스를 완료했다.
- 이어받아 카드 희귀도와 설계 가격 범위, 유물 2·포션 2 진열 및 구매를 4층 상점에 연결했다.
- 기존 버전 1 상점 저장은 계속 열리고 새 상점 저장은 혼합 진열·판매 상태를 보존한다.
- 실제 브라우저에서 4층까지 진행해 4+2+2 진열과 포션 구매/HUD를 확인했다.
- typecheck, 테스트 5파일 77개, build 통과. 콘솔 오류 0.
- 다음은 무이벤트 포션 사용 피드백 또는 런 덱 보기 UI다. 이미지 제작은 계속 보류한다.

## 15. 2026-06-28 추가 체크포인트 — 포션 피드백과 2막 전환 기준

- 에너지·드로우·회복 포션이 실제 적용량을 `BattleEvent`로 반환한다.
- 전투 화면은 해당 이벤트를 1.1초 토스트·방사형 플래시로 표시하며 모션 감소·라이브 알림을 지원한다.
- 실제 런에서 활력 주사를 획득해 4층 엘리트 전투에서 에너지 3→5, `에너지 +2` 피드백, 슬롯 제거를 확인했다.
- typecheck, 테스트 5파일 79개, build 통과. 콘솔 오류 0.
- 2막 판정은 **CONDITIONAL GO**다. 런 덱 보기 UI와 복수 시드 1막 풀런 QA를 마치면 즉시 2막으로 전환한다.
- 카드·적·포션 이미지 일괄 제작과 오디오는 2막 진입을 막지 않는다.

## 16. 2026-06-28 추가 체크포인트 — 런 덱 보기 UI

- 모든 비전투 화면의 상단 HUD에서 현재 덱 모달을 열 수 있다.
- 전체·종류·공격/스킬·강화·소멸 요약과 카드별 비용·설명·희귀도·중복 수량을 표시한다.
- 순수 집계 모델을 별도 파일로 분리하고 시작 덱·강화/소멸·미지 ID 테스트를 추가했다.
- 기존 `RunState(version: 1)` 저장 구조는 변경하지 않았다.
- typecheck, 테스트 6파일 82개, build 통과. 실제 4층 상점에서 모달과 Esc 닫기를 확인했고 콘솔 오류 0.
- 2막 전환 전 남은 작업은 복수 시드 1막 풀런 밸런스 QA 하나다.

## 17. 2026-06-30 추가 체크포인트 — 개별 캐릭터·적 라이브 연동

- 캐릭터 22종이 개별 텍스처를 사용한다. `jdd`만 기존 25프레임 모션을 유지하며, 나머지 21종은 전용 컷아웃과 절차형 공격 이펙트를 사용한다.
- 적 15종이 개별 텍스처를 사용하며 1·2·3막 모두 전용 6포즈 시트를 사용한다.
- `BattleStage.ts`, `BattleScreen.tsx`, `battleContent.ts`의 라이브 매핑이 갱신됐다.
- 테스트는 10파일 106개, typecheck와 production build까지 통과했다.
- 자동 브라우저 시각 검수는 Codex 인앱 브라우저 세션 오류로 미실행이다. 다음 게이트는 실제 화면 스케일·기준선 승인이다.
- 외부 배포·Supabase·신규 Aether 생성 없음.

## 18. 2026-06-30 추가 체크포인트 — 전투 비주얼 QA와 3막 명세

- 인앱 브라우저 연결을 복구해 실제 1막 전투와 1·2·3막 대표 적 배치를 검수했다.
- DEV 전용 `CombatVisualQa`에서 캐릭터 22종·적 15종 및 양측 공격 모션을 즉시 확인할 수 있다.
- 2막 시트의 공통 스케일을 336px 알파 폭 기준으로 수정했다.
- 심연의 돌격수·화력 지원선·전함 호위대·성간 전함의 전투 크기를 계층에 맞게 확대했다.
- 가로형 함선의 실제 세로 높이를 별도 추적해 공격·피격 위치를 보정했다.
- `tools/act3_enemy_keypose_prompts.json` 명세로 3막 적 5종의 6포즈 시트를
  생성·검수·라이브 승격했다.
- 이전 정적 파일 백업: `assets/game/enemy-motion-backup/20260630-172623/`.
- typecheck, 106개 테스트, build, 키포즈 처리기 py_compile 통과.
- `?combatQa=1`에서 3막 적 5종의 대기·공격·피격 위치를 확인했다.
  잘림·기준선 이탈·셀 혼입이 없어 3막 적 모션 트랙을 종료했다.

## 19. 2026-06-30 추가 체크포인트 — 전투 방향·종족 공격 수정

- 지두두 대기는 첫 프레임으로 고정하고 기존 공격 모션은 유지했다.
- P/Z 캐릭터의 절차형 공격은 투사체 대신 전진 칼질·에너지 참격을 사용한다.
- 잔해 총잡이는 런타임에서 좌향 반전하며 모든 애니메이션 스케일 변화에서도
  방향이 유지된다.
- typecheck, 106개 테스트, production build, 실제 화면 QA 통과.

## 20. 2026-06-30 추가 체크포인트 — 잔해 총잡이 좌향 재제작

- 런타임 반전 결과가 정면에 가깝게 읽혀 실제 좌향 3/4 측면 6포즈 시트로
  다시 제작했다.
- 리뷰: `assets/review/enemy-motion-v3/`.
- 라이브: `assets/game/enemy-motion/wasteland_gunner.png`.
- 백업: `assets/game/enemy-motion-backup/20260630-213549/`.
- 임시 반전 코드는 제거했다. 자동 QA·화면 QA·106테스트·build PASS.

## 21. 2026-06-30 추가 체크포인트 — 캐릭터 22종 시그니처 카드

- `src/content/signatureCards.ts`에 캐릭터별 고유 카드 22장을 정의했다.
- 각 캐릭터 시작 덱은 기본 `strike` 1장을 고유 카드로 교체해 10장을 유지한다.
- `CardDef.signatureFor`, `CharacterDef.signatureCard`로 소유 관계를 명시한다.
- 고유 카드는 종족 공용 보상·상점 풀에 들어가지 않으며 자동 강화가 가능하다.
- 캐릭터 선택, 덱 모달, 전투 손패, 휴식 강화 화면에서 시그니처를 구분한다.
- `runPersistence.ts`가 기존 버전 1 저장을 불러올 때 누락된 시그니처를
  기본 타격과 교체해 기존 런에도 기능을 적용한다.
- 카드 아트는 새로 만들지 않았고 플레이스홀더를 유지한다.
- typecheck, 10파일 110테스트, production build PASS.

## 22. 2026-07-01 추가 체크포인트 — 활성 로스터 20명으로 축소

- 사용자 지시로 `pado`(파도튜브), `peanut`(진땅콩)을 게임에서 삭제했다.
- 캐릭터·시그니처 카드·라이브 전투 PNG·향후 생성 배치에서 모두 제외했다.
- 활성 분포: T6·P5·Z9, 총 20명.
- 삭제 캐릭터의 과거 마스터·검수 자료는 게임이 로드하지 않는 제작 기록으로만 남긴다.
- 해당 캐릭터의 구형 저장은 더 이상 복원하지 않고 무효 저장으로 정리한다.
- typecheck, 10파일 112테스트, production build PASS.
