# Balance Check — 전투·카드·보상 경제 (2026-06-28)

/ `/balance-check` 결과 기록. 도메인: Combat + Economy.

## Data Sources Analyzed

- `src/content/battleContent.ts` — 카드 16종(+강화 변형 16) / 적 5종
- `src/game/run/RunEngine.ts` — 전투 골드·보상·상점 가격
- `src/game/run/actOne.ts` — 드래프트 풀(`ACT_ONE_CARD_POOL` 14)
- `docs/GAME_DESIGN.md` §6·§7·§10 — 설계 기준

## Health Summary: CONCERNS → 주요 2건 수정 적용

## Outliers (검출 → 조치)

| 항목 | 기준(설계) | 검출값 | 조치 |
|---|---|---|---|
| 일반전 골드 | 18~28 (§10) | 12~20 (`12+int(9)`) | **수정** → `18+int(11)` (18~28) |
| aimedshot 정조준 | 손익 명확(§1) | c1 피해 8 (8/에너지, 무손익 상위호환) | **수정** → 피해 7 |
| 엘리트 골드 | 35~45 | 35~45 | OK |
| 약화/취약 | -25% / +50% | floor(0.75) / floor(1.5) | OK |
| 시작 골드 | 75 | 75 | OK |

## 적용한 수정

1. `RunEngine.completeBattle` 일반전 골드 `12+int(9)` → `18+int(11)`. GAME_DESIGN §10 일치, 4층 상점 접근성 정상화.
2. `battleContent.ts` `aimedshot` 피해 8 → 7. 단발 효율 이상치 완화(타격 6 / 중사격 5.5·e 대비 무손익 압도 해소), 설계 기둥 "카드 선택엔 분명한 손익" 보존.

검증: typecheck·test(5파일 71개)·build 통과.

## 남은 항목(별도 묶음 — 미수정)

| 우선 | 이슈 | 메모 |
|---|---|---|
| 낮음 | 상점 가격이 **에너지 비용 기반**(`40+cost*15+int(11)`), 설계 §10은 **희귀도 기반**(일반 40~60/고급 70~95/희귀 110~145) | 카드에 rarity 필드 도입 후 가격 재설계 |
| 낮음(갭) | 상점이 카드만 판매(진열 4). 설계 §10은 **카드4·유물2·포션2** | 상점 유물·포션 진열 확장 묶음 |
| 낮음 | 카드 제거 가격 고정 75. 설계 "첫 75(+25씩)" 증가 미구현 | 노드당 1회 제거라 현재 영향 적음 |
| 정보 | guard(5/1 방어) < bulwark(8/1) 효율 | guard는 시작 기본 카드라 의도된 위계, 비degenerate |

## 비-이슈 확인

- 힘(strength)+다단(volley/flurry) 스케일링: 셋업 턴 필요해 1막 단기 전투에선 비degenerate. 의도된 아키타입.
- 적 HP 30~34(일반)/46(엘리트)/120(보스) vs 시작덱 화력 → 일반 TTK ~3~4턴, 적정.

재실행: 수정 후 `/balance-check` 재검증 권장(특히 상점 희귀도 도입 시).

## 후속 조치 — HANDOFF 28절

- 카드 `rarity` 도입과 희귀도별 가격 범위 적용 완료.
- 상점 카드4·유물2·포션2 진열 완료.
- 보유 유물 제외, starter 유물 제외, 포션 2슬롯 제한과 저장 왕복 검증 완료.
- 카드 제거 `75(+25씩)` 누적만 경제 갭으로 남음.
